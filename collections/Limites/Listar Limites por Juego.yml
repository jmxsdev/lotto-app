info:
  name: Listar Limites por Juego
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/limites/1"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

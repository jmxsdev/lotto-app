info:
  name: Ver Resultado
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/resultados/1"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Reprocesar Scraper (Dead-Letter)
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/resultados/reprocess"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
    - name: Authorization
      value: Bearer {{token}}
  body:
    type: json
    data: |-
      {
        "juego_id": 1,
        "fecha": "2026-08-13"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

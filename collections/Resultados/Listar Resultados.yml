info:
  name: Listar Resultados
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/resultados"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

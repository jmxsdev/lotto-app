info:
  name: Ejecutar Scraper (Super Master, Master)
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/resultados/scrape"
  headers:
    - name: Authorization
      value: Bearer {{token}}
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: '{"juego_id": 2}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 30000
  followRedirects: true
  maxRedirects: 5

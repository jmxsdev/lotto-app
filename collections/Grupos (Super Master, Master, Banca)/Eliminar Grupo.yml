info:
  name: Eliminar Grupo
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/api/grupos/1"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

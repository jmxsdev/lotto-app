info:
  name: Ver Grupo
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/grupos/1"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

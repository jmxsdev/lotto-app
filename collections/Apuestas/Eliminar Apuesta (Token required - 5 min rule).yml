info:
  name: Eliminar Apuesta (Token required - 5 min rule)
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/api/apuestas/1"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Resumen Estadistico
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/apuestas/resumen"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Historial Apuestas
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/apuestas/historial"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Ver Taquilla
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/taquillas/1"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

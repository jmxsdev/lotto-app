info:
  name: Actualizar Taquilla
  type: http
  seq: 3

http:
  method: PUT
  url: "{{baseUrl}}/api/taquillas/1"
  headers:
    - name: Authorization
      value: Bearer {{token}}
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "Taquilla 01 Actualizada",
        "active": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

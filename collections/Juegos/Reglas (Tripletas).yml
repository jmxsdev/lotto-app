info:
  name: Reglas (Tripletas)
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/api/juegos/2/reglas"
  headers:
    - name: Authorization
      value: Bearer {{token}}
    - name: X-Device-MAC
      value: "{{macAddress}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

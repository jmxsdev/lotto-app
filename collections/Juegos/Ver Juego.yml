info:
  name: Ver Juego
  type: http
  seq: 2

http:
  method: GET
  url: https://lotto.gzuz.dev/api/v1/juegos
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

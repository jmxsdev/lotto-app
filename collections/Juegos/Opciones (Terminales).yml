info:
  name: Opciones (Terminales)
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/api/juegos/3/opciones"
  headers:
    - name: Authorization
      value: Bearer {{token}}
    - name: X-Device-MAC
      value: "{{macAddress}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Opciones (Animalitos)
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/juegos/1/opciones"
  headers:
    - name: Authorization
      value: Bearer {{token}}
    - name: X-Device-MAC
      value: "{{macAddress}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

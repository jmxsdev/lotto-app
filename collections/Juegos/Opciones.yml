info:
  name: Opciones del Juego
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/juegos/1/opciones"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

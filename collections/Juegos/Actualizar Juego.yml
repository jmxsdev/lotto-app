info:
  name: Actualizar Juego
  type: http
  seq: 3

http:
  method: PUT
  url: "{{baseUrl}}/api/juegos/1"
  headers:
    - name: Authorization
      value: Bearer {{token}}
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "Animalitos Plus"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Reglas del Juego
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/juegos/2/reglas"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Horarios del Juego
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/juegos/1/horarios"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

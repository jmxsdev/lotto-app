info:
  name: Crear Cierre (Token required)
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/cierre"
  headers:
    - name: Authorization
      value: Bearer {{token}}
    - name: Content-Type
      value: application/json
    - name: X-Device-MAC
      value: ""
      disabled: true
  body:
    type: json
    data: |-
      {
        "taquilla_id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Ver Cierre (Token required)
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/cierre/{id}"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

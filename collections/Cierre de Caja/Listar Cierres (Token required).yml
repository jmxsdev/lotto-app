info:
  name: Listar Cierres (Token required)
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/cierre"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

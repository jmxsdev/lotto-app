info:
  name: Setear Tasa Activa
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/api/exchange-rates/1/set-active"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

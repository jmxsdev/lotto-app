info:
  name: Obtener Tasa Activa (sin token, pública)
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/exchange-rate/active"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

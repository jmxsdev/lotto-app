info:
  name: Obtener Tasa del BCV (Scraper)
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/api/exchange-rates/scrape"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 30000
  followRedirects: true
  maxRedirects: 5

info:
  name: Ver Tasa
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/exchange-rates/1"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Registrar Pago Triple Zulia
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/pagos"
  headers:
    - name: Authorization
      value: Bearer {{token}}
    - name: Content-Type
      value: application/json
    - name: X-Device-MAC
      value: "{{macAddress}}"
  body:
    type: json
    data: |-
      {
        "apuesta_id": 2,
        "amount_bs": 600,
        "amount_usd": 0,
        "moneda": "bs",
        "tipo": "egreso",
        "concepto": "Premio Triple Zulia"
      }
  auth: inherit

runtime:
  variables:
    - name: macAddress
      value: 00:1A:2B:3C:4D:5E

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

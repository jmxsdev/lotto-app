info:
  name: Ver Pago por Apuesta
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/pagos/1"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Ver Banca
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/bancas/1"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

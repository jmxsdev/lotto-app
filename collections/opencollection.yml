opencollection: 1.0.0

info:
  name: LottoApp API - 1

request:
  auth:
    type: bearer
    token: "{{token}}"
  variables:
    - name: baseUrl
      value: http://localhost:8000
    - name: token
      value: ""
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git

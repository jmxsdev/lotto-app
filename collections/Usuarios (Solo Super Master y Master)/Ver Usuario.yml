info:
  name: Ver Usuario
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/users/1"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
